import { createApp } from 'vue';
// Using vue <template> feature to enable static path analysing to reduce VNode creation
// import App from './Counter/App.vue';
// createApp(App).mount('#app');

// Using JSX - Entire VNode tree will be created each change => not recommend for production.
// import App2 from './Counter/App2.vue';
// createApp(App2).mount('#app');

// Using JSX - improvement - moving dynamic path to Component => each time change, only this component change.
// import App3 from './Counter/App3.vue';
// createApp(App3).mount('#app');

// import App4 from './Counter/App4.vue';
// createApp(App4).mount('#app');

// import App5 from './Counter/App5.vue';
// createApp(App5).mount('#app');

// import App from '@/ComponentWIthProp/App.vue';
// createApp(App).mount('#app');

// import App from '@/ComponentWIthProp/App1.vue';
// createApp(App).mount('#app');

// import App from '@/ComponentWIthProp/App2.vue';
// createApp(App).mount('#app');

// import App from '@/ComponentWIthProp/App3.vue';
// createApp(App).mount('#app');

// import App from '@/ComponentWIthProp/App4.vue';
// createApp(App).mount('#app');
